# Capture record schema

Use one record per packet with observed timestamp, build hash, direction, opcode, byte length, sanitized hex payload, client state, server response and reproduction steps. Never commit credentials, live session tokens or player personal data.
