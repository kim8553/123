# Protocol change evidence requirements

For each mismatch, capture request direction, opcode, framing, payload length, field offsets/types, relevant session state, expected response and actual response. Record hash/build identifiers for binaries and a repeatable regression case. Mark unobserved values as unknown. Do not disable authentication or encryption to manufacture matching traffic.
